#include <stdio.h>
#include <stdlib.h>

int main ()
{
	//generate an array of 2^(N) random bytes
	//LOOP
		//start the clock
		//read all the bytes like, 10 times?
		//stop the clock
		//shuffle all of the bytes around, somehow
		//compute N/time elapsed
		//print it!
		//back to the top!
	return
}